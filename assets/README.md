# assets

Drop the background-music file here as **anthem.mp3** (exact name).
The site references `assets/anthem.mp3` from the music toggle button (bottom-left).
Any MP3 works; keep it reasonably small (a few MB) for fast loading.
